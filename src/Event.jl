module Event
import CSV

function EventFreq()
    #create matrix of events with event code, initial outs, initial base code, containing outcomes + freqs
end

function PlateAppearance()
    #generate event from game state and return outcomes
end

function NonBatter()
    #generate nbe based on game state
end

end
